  The Command I ran to clone a branch of a repo on VS Code from Github is as follows:

        git clone -b 'branch-name'  --single-branch 'repo address bar link'

created app in current directory by running '       ng new "appName" --directory ./       'fj
